fn process_item(id):

    let item = SELECT id, name, price, in_stock
               FROM items
               WHERE id = id;

    if (SELECT price FROM item > 2):
        INSERT INTO cart SELECT id, name, price FROM item;
    else:
        INSERT INTO skipped VALUES (id, 'too_cheap');